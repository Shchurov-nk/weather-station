{$DOMAIN} {
	# The api keeps its two routes; everything else (including websockets,
	# which reverse_proxy upgrades automatically) goes to the dashboard.
	handle /sensor* {
		reverse_proxy api:8000
	}
	handle /table {
		reverse_proxy api:8000
	}
	handle {
		reverse_proxy dashboard:8501
	}
}
